<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="atlas.apple">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name="MyApplication">
        <activity
            android:name=".Recipe"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".PopupEnvironment"
            android:theme="@style/AppTheme.customTheme">

        </activity>

        <activity android:name=".PopupCarbon"
            android:theme="@style/AppTheme.customTheme">

        </activity>

        <activity android:name=".PopupDifficulty"
            android:theme="@style/AppTheme.customTheme">

        </activity>

        <activity android:name=".PopupPrice"
            android:theme="@style/AppTheme.customTheme">

        </activity>

        <activity android:name=".PopupWater"
            android:theme="@style/AppTheme.customTheme">

        </activity>

        <activity android:name=".PopupTime"
            android:theme="@style/AppTheme.customTheme">

        </activity>

        <activity android:name=".PopupRanking"
            android:theme="@style/AppTheme.customTheme">

        </activity>

    </application>

</manifest>
